document.body.onkeydown = function(keypress_event){
  var key = keypress_event.key;

dodocument.addEventListener('keydown', function(keypress_event) {
  if (key == "a" ) {
    document.getElementById('audio').play();
    document.getElementByClassName("image1").style.display = "none";
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "b" ) {
    document.getElementByClassName("image2").style.display = "none";
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "c" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image3').style.display = inline;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "d" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image4').style.display = inline;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "e" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image5').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "f" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image6').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "g" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image7').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "h" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image8').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "i" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image9').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "j" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image10').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "k" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image11').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "l" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image12').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "m" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image13').style.display = block;
    }
  });

document.addEventListener('keydown', function(keypress_event) {
  if (key == "n" ) {
    document.getElementById('audio').play();
    document.getElementByClassName('image14').style.display = block;
    }
  });
};